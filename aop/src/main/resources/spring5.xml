<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 扫描指定包,将带有@Componet注解的类自动定义为Spring Bean -->
    <context:component-scan base-package="aop.pointCutAdvisor"/>

    <!-- 配置一个切面
        RegexpMethodPointcutAdvisor 正则表达式匹配
        DefaultPointcutAdvisor 默认切面
        NameMatchMethodPointcutAdvisor 根据方法名称进行匹配
        StaticMethodMatcherPointcutAdvisor 用于匹配静态方法的切面
   -->
    <bean id="greetingAdvisor" class="org.springframework.aop.support.RegexpMethodPointcutAdvisor">
        <!-- 增强 -->
        <property name="advice" ref="greetingAroundAdvise"/>
        <!-- 使用切面就必须配置切点,只有切点才会被增强 -->
        <property name="pattern" value="aop.pointCutAdvisor.GreetingImpl.good.*"/>
    </bean>

    <!-- 自动代理扫描切面-->
    <bean class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator">
        <property name="optimize" value="true"/>
    </bean>
</beans>